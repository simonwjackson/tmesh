{
  "name": "tmesh",
  "version": "0.0.0",
  "author": "Simon W. Jackson",
  "repository": {
    "type": "git",
    "url": "https://github.com/simonwjackson/tmesh"
  },
  "main": "src/index.ts",
  "type": "module",
  "bin": {
    "tmesh": "src/cli.ts",
    "server-select": "src/server-select.ts",
    "app-select": "src/app-select.ts",
    "popup-shell": "src/popup-shell.ts"
  },
  "description": "Effortlessly manage tmux sessions across multiple hosts",
  "keywords": [
    "tmux",
    "mosh",
    "ssh",
    "terminal",
    "multiplexer",
    "session-management"
  ],
  "license": "GPL-2.0-only",
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "bun build src/cli.ts --compile --outfile=dist/tmesh && bun build src/server-select.ts --compile --outfile=dist/server-select && bun build src/app-select.ts --compile --outfile=dist/app-select && bun build src/popup-shell.ts --compile --outfile=dist/popup-shell",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "bun x tsc --noEmit"
  },
  "dependencies": {
    "yaml": "^2.8.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
